# AiKey CLI Spec (stub)

The authoritative CLI Platform spec is maintained in the roadmap repository:

- `specs/platform/CLI_SPEC.md`

Please edit and review the CLI spec there. This file is kept only as a pointer for discoverability in the `aikey-cli` codebase.
